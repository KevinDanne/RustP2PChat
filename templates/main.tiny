vstack:
    border:
        vstack:
            for [data: {{ messages }}, binding: message]:
                text [id: {{ message.msg }}, foreground: {{ message.foreground }}]: "{{ message.foreground }}"

    border:
        text: "{{ input }}"
